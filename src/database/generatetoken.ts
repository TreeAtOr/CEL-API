import { ObjectId } from "mongoose";

export function generateToken(_id: ObjectId,length: number) {
    const memoryble : boolean = false;
    return ("GenerateToken function is not implemented.");
}